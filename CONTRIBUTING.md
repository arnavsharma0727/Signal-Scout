# Contributing

Keep the product evidence-first and honest. Do not add demo companies, fake URLs, synthetic stories, fabricated translations, or investment-oriented claims. Add tests using only neutral structured values such as `entity_a` and `example.invalid`.
