# Security

Report vulnerabilities privately to the contact configured in `NEXT_PUBLIC_CONTACT_EMAIL`. Never expose Supabase service-role keys or connector credentials to browser code. Internal job endpoints must require `CRON_INTERNAL_SECRET`.
