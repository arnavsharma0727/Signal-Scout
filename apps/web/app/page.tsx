import Link from 'next/link';
import {ArrowUpRight,BookOpen,Download,FileText,Globe2,Radar,ShieldCheck} from 'lucide-react';
import {publishedDivergences} from '../lib/public-data';
import {syntheticPreviewDocuments,syntheticTopicRows} from '../lib/synthetic-preview';
const nav=[['Divergence Radar','/'],['Companies','/companies'],['Candidates','/candidates'],['Methodology','/methodology'],['Sources','/sources']];
export default async function Home(){const live=await publishedDivergences();const preview=process.env.NODE_ENV!=='production'&&process.env.ENABLE_SYNTHETIC_PREVIEW==='true';const rows=preview?syntheticTopicRows():[];return <div className="grid-bg min-h-screen"><header className="shell flex h-20 items-center justify-between border-b border-line"><Link href="/" className="flex items-center gap-3"><span className="flex h-8 w-8 items-center justify-center rounded-lg bg-accent text-base"><Radar size={18}/></span><span className="font-extrabold tracking-tight">SIGNAL SCOUT</span></Link><nav className="hidden gap-7 text-sm text-muted md:flex">{nav.map(([n,h])=><Link key={h} href={h} className={h==='/'?'text-ink':''}>{n}</Link>)}</nav><Link href="/about" className="btn">About <ArrowUpRight size={14}/></Link></header><main className="shell pb-24 pt-20"><div className="max-w-4xl"><div className="eyebrow mb-5">Korea ↔ United States / conversation intelligence</div><h1 className="text-5xl font-extrabold leading-[1.05] tracking-[-.04em] md:text-7xl">Find the imbalance.<br/><span className="text-accent">Investigate the conversation.</span></h1><p className="mt-7 max-w-3xl text-lg leading-8 text-muted">Signal Scout compares Korean-language and American news/forum source samples to show where topic frequency or framing differs.</p><p className="mt-3 max-w-3xl text-sm leading-7 text-muted">A difference is a knowledge starting point—not a trading signal, financial conclusion, or claim that one market is correct.</p></div>{preview?<PreviewPanel rows={rows}/>:live.length===0?<EmptyPanel/>:<LivePanel divergences={live}/>}<div className="mt-5 grid gap-5 md:grid-cols-3"><Info icon={<FileText/>} title="Source-aware" text="Keep original language, source type, URL, time, and provenance visible." href="/sources"/><Info icon={<Globe2/>} title="Korea ↔ U.S." text="Compare configured Korean sources with a sampled American source set." href="/methodology"/><Info icon={<BookOpen/>} title="Knowledge brief" text="See what differs, what may explain it, and what to verify next." href="/methodology"/></div><div className="mt-8 flex items-center gap-2 border-t border-line pt-6 text-xs text-muted"><ShieldCheck size={15} className="text-accent"/> Research only — not investment advice or a trading recommendation.</div></main></div>}
function PreviewPanel({rows}:{rows:ReturnType<typeof syntheticTopicRows>}){return <section className="mt-12"><div className="mb-5 border border-amber-400/40 bg-amber-400/10 p-4 text-sm text-amber-200"><strong>Development preview:</strong> all documents and measurements below are synthetic test data. They are not real Korean or American news/forum content and are never shown in production.</div><div className="grid gap-5 lg:grid-cols-[1.1fr_.9fr]"><section className="panel overflow-hidden"><div className="border-b border-line p-6"><div className="eyebrow">Conversation imbalance radar</div><div className="mt-2 flex items-center justify-between"><p className="text-sm text-muted">Synthetic 3-day window · KR forum/news vs U.S. forum/news</p><span className="mono text-xs text-accent">DEV ONLY</span></div></div><div className="divide-y divide-line">{rows.map(r=><div className="grid gap-3 p-5 md:grid-cols-[1fr_auto]" key={r.topic}><div><div className="text-sm font-bold">{r.topic}</div><div className="mt-1 text-xs text-muted">KR {r.krCount} docs · U.S. {r.usCount} docs · synthetic sample</div></div><div className="text-right"><div className="mono text-xl text-accent">{r.gap>=0?'+':''}{r.gap.toFixed(2)}</div><div className="text-[10px] uppercase tracking-wider text-muted">KR − U.S. share</div></div></div>)}</div></section><section className="panel p-6"><div className="eyebrow mb-6">Knowledge brief preview</div><h2 className="text-xl font-bold">What differs?</h2><p className="mt-3 text-sm leading-7 text-muted">In this synthetic sample, the Korean source set contains more documents tagged with pricing and refund topics than the U.S. sample. This is a UI demonstration only.</p><h2 className="mt-7 text-xl font-bold">What to verify next</h2><ul className="mt-3 space-y-3 text-sm leading-6 text-muted"><li>• Read the original Korean source text.</li><li>• Check whether both samples cover the same time window.</li><li>• Look for syndicated or duplicated reporting.</li><li>• Confirm the topic taxonomy fits the documents.</li></ul></section></div><div className="panel mt-5 overflow-hidden"><div className="border-b border-line p-5"><div className="eyebrow">Synthetic source cards</div></div><div className="grid gap-4 p-5 md:grid-cols-2">{syntheticPreviewDocuments.map(d=><div className="rounded-lg border border-line p-4" key={d.id}><div className="flex justify-between text-[10px] uppercase tracking-wider text-muted"><span>{d.market} · {d.sourceType}</span><span>{d.language}</span></div><h3 className="mt-3 text-sm font-bold">{d.title}</h3><p className="mt-2 text-xs leading-5 text-muted">{d.excerpt}</p></div>)}</div></div></section>}
function EmptyPanel(){return <div className="panel mt-16 flex min-h-[330px] flex-col justify-between p-8 md:p-10"><div><div className="eyebrow mb-8">Conversation imbalance radar</div><h2 className="max-w-lg text-3xl font-bold tracking-tight">No live source data configured.</h2><p className="mt-4 max-w-xl leading-7 text-muted">Signal Scout requires an authorized Korean source set and a sampled American source set. It does not invent news, forum posts, translations, or measurements.</p></div><div><Link className="btn btn-primary" href="/sources">Configure sources <ArrowUpRight size={15}/></Link></div></div>}
function LivePanel({divergences}:{divergences:any[]}){return <section className="panel mt-16 overflow-hidden"><div className="border-b border-line p-6"><div className="eyebrow">Published conversation differences</div><p className="mt-2 text-sm text-muted">Results from configured Korean and American source sets.</p></div><div className="divide-y divide-line">{divergences.map(d=><Link href={`/divergences/${d.id}`} key={d.id} className="grid gap-3 p-6 transition hover:bg-white/[.03] md:grid-cols-[1fr_auto]"><div><div className="text-xs uppercase tracking-wider text-accent">{d.local_market_code} · {d.topic} · {d.time_window}</div><h2 className="mt-2 text-lg font-bold">Cross-Market Research Starting Point</h2><p className="mt-1 text-sm text-muted">Narrative Topic Difference: {typeof d.narrative_topic_gap==='number'?d.narrative_topic_gap.toFixed(3):'unavailable'} · Evidence: {d.evidence_level??'unspecified'}</p></div><div className="self-center text-right"><div className="mono text-2xl text-accent">{d.research_priority_score??'—'}</div><div className="text-xs text-muted">priority</div></div></Link>)}</div></section>}
function Info({icon,title,text,href}:{icon:React.ReactNode,title:string,text:string,href:string}){return <Link href={href} className="panel block p-6 transition hover:border-accent"><div className="mb-5 text-accent">{icon}</div><h3 className="font-bold">{title}</h3><p className="mt-2 text-sm leading-6 text-muted">{text}</p><span className="mt-5 inline-flex items-center gap-1 text-xs font-bold">Learn more <ArrowUpRight size={13}/></span></Link>}
