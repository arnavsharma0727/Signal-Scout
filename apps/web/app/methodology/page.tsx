import Link from 'next/link';
export default function Methodology(){return <Page title="Methodology" eyebrow="How Signal Scout thinks"><div className="grid gap-5 md:grid-cols-2">{[['Candidate vs Research Lead','Candidates are triage items. A Cross-Market Research Starting Point requires evidence thresholds: local weighted total ≥ 1.5, local topic evidence ≥ 0.75, at least 2 independent local domains, average entity confidence ≥ 0.75, freshness, and either a Tier 1/2 source or 3 independent Tier 3+ sources.'],['Weighted topic share','Each document receives its source-tier weight multiplied by entity-match confidence. Duplicate or syndicated documents receive a configurable default 0.10 multiplier. Topic share is weighted topic evidence divided by all weighted company-related evidence in the same source set and window.'],['Smoothing and topic difference','Beta/Laplace smoothing uses α=1 and β=1: (topic weight + α) / (total weight + α + β). Narrative Topic Difference is the smoothed local share minus the smoothed English share. It describes configured information environments; it is not a forecast.'],['Cross-Market Research Priority','A 0–100 triage aid: 30% recency, 25% source quality, 20% independent local concentration, 15% difference strength, 10% profile/evidence quality, less penalties for ambiguity, duplicates, and staleness. Recency uses a 72-hour half-life by default.'],['Evidence first','Original title, excerpt, URL, time, language, market, source type, domain, tier, match confidence, and freshness remain visible. Translations are marked derived and only come from returned source text.'],['Validate independently','Read the original source, check official local statements and investor-relations materials, review filings, compare competitor context, seek disconfirming evidence, and revisit the next relevant earnings commentary.']].map(([h,p])=><section className="panel p-7" key={h}><h2 className="text-xl font-bold">{h}</h2><p className="mt-3 leading-7 text-muted">{p}</p></section>)}</div><p className="mt-8 text-sm text-muted">Selection, coverage, platform-demographic, language/entity, timing, small-sample, source-dependence, materiality, and multiple-testing biases can create false positives. These are triage heuristics, not predictive financial models.</p></Page>}
function Page({title,eyebrow,children}:{title:string,eyebrow:string,children:React.ReactNode}){return <div className="min-h-screen"><header className="shell flex h-20 items-center justify-between border-b border-line"><Link href="/" className="font-extrabold">SIGNAL SCOUT</Link><Link href="/" className="text-sm text-muted">← Research Radar</Link></header><main className="shell py-16"><div className="eyebrow mb-4">{eyebrow}</div><h1 className="mb-10 text-4xl font-extrabold tracking-tight">{title}</h1>{children}</main></div>}
