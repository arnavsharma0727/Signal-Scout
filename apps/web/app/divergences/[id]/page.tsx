import Link from 'next/link';
export default function DivergenceDetail(){return <div className="min-h-screen"><header className="shell flex h-20 items-center justify-between border-b border-line"><Link href="/" className="font-extrabold">SIGNAL SCOUT</Link><Link href="/" className="text-sm text-muted">← Divergence Radar</Link></header><main className="shell py-16"><div className="eyebrow mb-4">Cross-Market Research Starting Point</div><h1 className="max-w-3xl text-4xl font-extrabold tracking-tight">No divergence selected.</h1><div className="panel mt-10 max-w-3xl p-8"><h2 className="text-xl font-bold">Insufficient evidence to display a result.</h2><p className="mt-3 leading-7 text-muted">A detail page appears only for a published divergence backed by configured local and sampled English source sets. No source documents, calculations, translations, or statistics are invented for empty states.</p><p className="mt-6 text-sm font-semibold text-accent">Comparison unavailable until real profiles and authorized sources are configured.</p></div><div className="mt-10 grid max-w-3xl gap-4 md:grid-cols-3">{['Calculation Summary','Verified Evidence','Validation Checklist'].map(t=><div className="panel p-5" key={t}><h2 className="font-bold">{t}</h2><p className="mt-2 text-sm text-muted">Available when evidence thresholds are met.</p></div>)}</div><p className="mt-8 text-xs text-muted">Research only — not investment advice or a trading recommendation.</p></main></div>}
